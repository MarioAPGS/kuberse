# Vault secrets expected by this plugin
# These must be seeded in Vault before enabling vault integration.
#
# Path: secret/hello/config
# Keys:
#   - APP_SECRET_KEY: Application secret key (generate with: openssl rand -hex 32)
#
# Path: secret/hello/db
# Keys:
#   - PG_CONNECTION_STRING: postgresql://hello:<password>@postgres.platform.svc.cluster.local:5432/hello
